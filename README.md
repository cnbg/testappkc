# KC test app

- ### create and populate db with sql file attached
- ### run in terminal inside back folder 'php -S localhost:8080' to run backend apis
- ### run front as http://localhost/index.html

- db credentials can be set in back/.env file
